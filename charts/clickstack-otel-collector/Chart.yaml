apiVersion: v2
name: clickstack-otel-collector
description: ClickStack OpenTelemetry Collector — OTLP gateway that writes telemetry directly into ClickHouse (local or external). Deployed only when global.deploymentMode=external.
type: application
version: 0.1.4
appVersion: "2.31.0"
