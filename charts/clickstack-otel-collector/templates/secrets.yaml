{{- if eq (include "neuraltrust-platform.isExternal" .) "true" }}
{{- if and (include "neuraltrust-platform.autoGenerateSecrets" .) (not .Values.global.preserveExistingSecrets) }}
{{- $existing := (lookup "v1" "Secret" .Release.Namespace (printf "%s-secrets" (include "clickstack-otel-collector.fullname" .))) }}
{{- if or .Release.IsInstall $existing .Values.otlpAuthToken }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clickstack-otel-collector.fullname" . }}-secrets
  annotations:
    helm.sh/resource-policy: keep
  labels:
    {{- include "clickstack-otel-collector.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- /* AUT-636: an explicit endpoint still wins for this consumer alone; empty
         composes one from global.clickhouse (host + HTTP port, scheme following
         global.clickhouse.tls), then the in-cluster default. This chart is the only
         ClickHouse consumer configured by URL rather than host+port. */}}
  {{- $ch := include "neuraltrust-platform.clickhouse.resolve" (dict "local" .Values.clickhouse "global" .Values.global) | fromYaml }}
  CLICKHOUSE_ENDPOINT: {{ include "clickstack-otel-collector.clickhouseEndpoint" . | quote }}
  CLICKHOUSE_USER: {{ include "neuraltrust-platform.resolveSecret" (dict "value" $ch.user "existingSecret" $existing "secretKey" "CLICKHOUSE_USER" "length" 16) | quote }}
  {{- /* CLICKHOUSE_PASSWORD is sourced from the shared ClickHouse secret
         (.Values.clickhouse.existingSecret) via the Deployment env, so the collector
         authenticates with the same credential as DataCore/AlertEngine. Not stored here. */}}
  {{- /* OTLP_AUTH_TOKEN authenticates the collector's receivers. The same value
         is also emitted as OTEL_EXPORTER_OTLP_HEADERS so in-cluster senders
         (TrustGuard / AgentGateway) can secretKeyRef a ready-to-use header
         without duplicating the token into per-service Secrets. */}}
  {{- $otlpToken := include "neuraltrust-platform.resolveSecret" (dict "value" .Values.otlpAuthToken "existingSecret" $existing "secretKey" "OTLP_AUTH_TOKEN" "length" 48) }}
  OTLP_AUTH_TOKEN: {{ $otlpToken | quote }}
  OTEL_EXPORTER_OTLP_HEADERS: {{ printf "authorization=%s" $otlpToken | quote }}
{{- end }}
{{- end }}
{{- end }}
