{{- /*
  Optional PodDisruptionBudget for the data-plane api.
  Defaults OFF — enable per component:

    dataPlane.components.<component>.podDisruptionBudget:
      enabled: true
      minAvailable: 1
*/ -}}
{{- if and .Values.dataPlane .Values.dataPlane.enabled }}
{{- $dp := .Values.dataPlane }}
{{- $apiEnabled := eq (include "neuraltrust-platform.dataPlaneApiV2.enabled" .) "true" }}
{{- if and $apiEnabled $dp.components $dp.components.api $dp.components.api.enabled }}
{{- $pdb := default dict $dp.components.api.podDisruptionBudget }}
{{- $replicas := default 2 $dp.components.api.replicas }}
{{- $autoscaling := default dict $dp.components.api.autoscaling }}
{{- if $autoscaling.enabled }}
  {{- $replicas = default 1 $autoscaling.minReplicas }}
{{- end }}
{{- if and $pdb.enabled (gt ($replicas | int) 1) }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: data-plane-api
  namespace: {{ .Release.Namespace }}
  labels:
    app: api
    plane: data
spec:
  {{- if hasKey $pdb "maxUnavailable" }}
  maxUnavailable: {{ $pdb.maxUnavailable }}
  {{- else if hasKey $pdb "minAvailable" }}
  minAvailable: {{ $pdb.minAvailable }}
  {{- else }}
  maxUnavailable: 1
  {{- end }}
  selector:
    matchLabels:
      app: api
{{- end }}
{{- end }}
{{- end }}
