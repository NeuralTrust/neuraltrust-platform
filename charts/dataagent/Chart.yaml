apiVersion: v2
name: dataagent
description: DataAgent — customer-side data-residency agent. Outbound-only gRPC to the SaaS DataBridge; runs typed read-only queries on a local Postgres. Deployed only when global.platformVersion=v2 AND global.deploymentMode=hybrid.
type: application
version: 0.1.2
appVersion: "v0.1.1"
