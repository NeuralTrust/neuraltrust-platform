apiVersion: v2
name: dataagent
description: DataAgent library — customer-side data-residency agent helpers. Umbrella renders one instance per enabled hybrid product (trustgate / trustguard).
type: library
version: 0.1.16
appVersion: "v0.6.1"
