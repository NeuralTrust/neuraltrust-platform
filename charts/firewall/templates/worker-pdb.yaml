{{- if eq (include "firewall.enabled" .) "true" }}
{{- $defaults := .Values.firewall.workerDefaults }}
{{- range $workerName, $workerCfg := .Values.firewall.workers }}
{{- if $workerCfg.enabled }}
{{- $pdb := $workerCfg.podDisruptionBudget | default $defaults.podDisruptionBudget }}
{{- $autoscaling := $workerCfg.autoscaling | default $defaults.autoscaling | default dict }}
{{- $replicas := $workerCfg.replicas | default ($defaults.replicas | default 1) }}
{{- if $autoscaling.enabled }}
  {{- $replicas = default 1 $autoscaling.minReplicas }}
{{- end }}
{{- if and $pdb $pdb.enabled (gt ($replicas | int) 1) }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ $workerName }}-worker
  labels:
    {{- include "firewall.worker.labels" (dict "name" $workerName "context" $) | nindent 4 }}
spec:
  {{- if hasKey $pdb "maxUnavailable" }}
  maxUnavailable: {{ $pdb.maxUnavailable }}
  {{- else if hasKey $pdb "minAvailable" }}
  minAvailable: {{ $pdb.minAvailable }}
  {{- else }}
  maxUnavailable: 1
  {{- end }}
  selector:
    matchLabels:
      {{- include "firewall.worker.selectorLabels" (dict "name" $workerName) | nindent 6 }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
