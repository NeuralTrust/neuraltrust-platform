{{- if and .Values.aispm.enabled .Values.aispm.worker.autoscaling .Values.aispm.worker.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: aispm-worker
  labels:
    {{- include "aispm.labels" (dict "component" "worker" "context" .) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: aispm-worker
  minReplicas: {{ .Values.aispm.worker.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.aispm.worker.autoscaling.maxReplicas | default 4 }}
  metrics:
    {{- if .Values.aispm.worker.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.aispm.worker.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.aispm.worker.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.aispm.worker.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
