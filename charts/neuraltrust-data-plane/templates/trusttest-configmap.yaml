{{- /* platform-v2: legacy component disabled when global.platformVersion=v2 */ -}}
{{- if not (eq (include "neuraltrust-platform.isV2" .) "true") }}
{{- if eq (include "data-plane.api.trustTestConfig.enabled" .) "true" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: data-plane-trusttest-config
  labels:
    app: api
    plane: data
data:
  .trusttest_config.json: |-
{{ .Files.Get "files/.trusttest_config.json" | indent 4 }}
{{- end }} 
{{- end }}
