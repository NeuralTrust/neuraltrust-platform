apiVersion: v2
name: trustguard
description: TrustGuard — AI security detector engine, split into control-plane (admin API) + data-plane (runtime /v1/guard). Deployed only when global.platformVersion=v2.
type: application
version: 0.1.3
appVersion: "v0.9.1"
