apiVersion: v2
name: trustlens
description: TrustLens (WIP) — analytics/inventory backend. Single image, two runtimes (api + River worker), Postgres-only. Opt-in via trustlens.enabled.
type: application
version: 0.1.1
appVersion: "0.1.0"
