NeuralTrust Platform has been deployed!

{{- if .Values.global.autoGenerateSecrets }}
Secrets have been auto-generated for this deployment.
  - Existing secrets are preserved on upgrades.
  - To view generated secrets: kubectl get secrets -n {{ .Release.Namespace }}
{{- end }}

{{- $isExternal := eq (include "neuraltrust-platform.isExternal" .) "true" }}
{{- $isSaas := eq (include "neuraltrust-platform.isSaas" .) "true" }}
{{- $dataagentEnabled := eq (include "neuraltrust-platform.dataagentEnabled" .) "true" }}
{{- $trustgateEnabled := eq (include "neuraltrust-platform.product.enabled" (dict "ctx" . "product" "trustgate")) "true" }}
{{- $trustguardEnabled := eq (include "neuraltrust-platform.product.enabled" (dict "ctx" . "product" "trustguard")) "true" }}
{{- $dataPlaneEnabled := eq (include "neuraltrust-platform.product.enabled" (dict "ctx" . "product" "dataPlane")) "true" }}
{{- $platform := .Values.global.platform | default "gcp" }}
{{- $ipField := "ip" }}
{{- if eq $platform "aws" }}
  {{- $ipField = "hostname" }}
{{- end }}
{{- $ag := .Values.agentgateway | default dict }}
{{- $mcpEnabled := dig "mcp" "enabled" true $ag }}
{{- $cpApi := index .Values "control-plane-api" | default dict }}
{{- $cpApp := index .Values "control-plane-app" | default dict }}
{{- $dpValues := index .Values "data-plane-api" | default dict }}

Deployment profile: Platform v2 — {{ if $isSaas }}saas (customer-owned central control plane; remote data planes enrol){{ else if $isExternal }}external (control + data plane + in-cluster analytics){{ else }}hybrid (data plane in-cluster; hosted control plane){{ end }}.

{{- if $isExternal }}
{{- if .Values.infrastructure.clickhouse.deploy }}
ClickHouse has been deployed in this release.
{{- else }}
Using external ClickHouse at {{ .Values.infrastructure.clickhouse.external.host }}:{{ .Values.infrastructure.clickhouse.external.port }}
{{- end }}
{{- end }}

{{- $redisDeploy := true }}
{{- if hasKey (.Values.global.redis | default dict) "deploy" }}{{- $redisDeploy = .Values.global.redis.deploy }}{{- end }}
{{- if $redisDeploy }}
Redis has been deployed in this release (service "redis").
{{- else if .Values.global.redis.host }}
Using external Redis at {{ .Values.global.redis.host }}:{{ .Values.global.redis.port }}
{{- end }}

{{- if eq (include "neuraltrust-platform.postgresql.deploy" .) "true" }}
PostgreSQL has been deployed in this release (service "control-plane-postgresql").
{{- else if and (eq (include "neuraltrust-platform.postgresRequired" .) "true") (.Values.global.postgresql.host) }}
Using external PostgreSQL at {{ .Values.global.postgresql.host }}:{{ .Values.global.postgresql.port }}
{{- else if eq (include "neuraltrust-platform.telemetry.raw" .) "remote" }}
PostgreSQL is not installed (global.telemetry.raw=remote). Raw telemetry leaves over OTLP.
{{- end }}

Enabled v2 components:
{{- if $trustgateEnabled }}
  - AgentGateway proxy{{ if $mcpEnabled }} + MCP server{{ end }} (runtime data plane)
{{- end }}
{{- if $trustguardEnabled }}
  - TrustGuard runtime (data plane)
  - Firewall
{{- end }}
{{- if $dataPlaneEnabled }}
  - data-plane-api (analytics shim)
{{- end }}
{{- if $dataagentEnabled }}
  - DataAgent per selected TrustGate/TrustGuard product
{{- end }}
{{- if $isExternal }}
{{- if $trustgateEnabled }}
  - AgentGateway Admin (on-prem control plane)
{{- end }}
{{- if $trustguardEnabled }}
  - TrustGuard Control Plane (on-prem control plane)
{{- end }}
  - Control Plane API + App (on-prem console)
  - DataCore + ClickStack OTel Collector (self-hosted analytics)
  - AlertEngine
{{- if $isSaas }}
  - DataBridge (southbound L4 for remote data planes)
  - ClickStack ingest gateway (public OTLP)
{{- end }}
{{- end }}
{{- if .Values.trustlens.enabled }}
  - TrustLens
{{- end }}

Ingress endpoints:
  kubectl get ingress -n {{ .Release.Namespace }}
{{- if $trustgateEnabled }}
{{- $agGwHost := include "neuraltrust-platform.ingress.host" (dict "host" (dig "ingress" "dataPlane" "host" "" $ag) "prefix" "gateway" "global" .Values.global) }}
{{- if $agGwHost }}
  - AgentGateway Gateway: https://{{ $agGwHost }}
{{- else }}
  - AgentGateway Gateway: kubectl get ingress agentgateway-gateway -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].{{ $ipField }}}'
{{- end }}
{{- if $mcpEnabled }}
{{- $agMcpHost := include "neuraltrust-platform.ingress.host" (dict "host" (dig "ingress" "mcp" "host" "" $ag) "prefix" "mcp" "global" .Values.global) }}
{{- if $agMcpHost }}
  - AgentGateway MCP:     https://{{ $agMcpHost }}
{{- else }}
  - AgentGateway MCP:     kubectl get ingress agentgateway-mcp -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].{{ $ipField }}}'
{{- end }}
{{- end }}
{{- end }}
{{- if $trustguardEnabled }}
{{- $tg := .Values.trustguard | default dict }}
{{- $tgHost := include "neuraltrust-platform.ingress.host" (dict "host" (dig "ingress" "dataPlane" "host" "" $tg) "prefix" "trustguard" "global" .Values.global) }}
{{- if $tgHost }}
  - TrustGuard:           https://{{ $tgHost }}
{{- else }}
  - TrustGuard:           kubectl get ingress trustguard -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].{{ $ipField }}}'
{{- end }}
{{- end }}
{{- if $isExternal }}
{{- if $trustgateEnabled }}
{{- $agAdminHost := include "neuraltrust-platform.ingress.host" (dict "host" (dig "ingress" "controlPlane" "host" "" $ag) "prefix" "admin" "global" .Values.global) }}
{{- if $agAdminHost }}
  - AgentGateway Admin:   https://{{ $agAdminHost }}
{{- else }}
  - AgentGateway Admin:   kubectl get ingress agentgateway-admin -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].{{ $ipField }}}'
{{- end }}
{{- end }}
{{- if $trustguardEnabled }}
{{- $tg := .Values.trustguard | default dict }}
{{- $tgAdminHost := include "neuraltrust-platform.ingress.host" (dict "host" (dig "ingress" "controlPlane" "host" "" $tg) "prefix" "trustguard-admin" "global" .Values.global) }}
{{- if $tgAdminHost }}
  - TrustGuard Admin:     https://{{ $tgAdminHost }}
{{- else }}
  - TrustGuard Admin:     kubectl get ingress trustguard-admin -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].{{ $ipField }}}'
{{- end }}
{{- end }}
{{- $cpApiHost := include "neuraltrust-platform.ingress.host" (dict "host" (dig "controlPlane" "components" "api" "host" "" $cpApi) "prefix" "api" "global" .Values.global) }}
{{- if $cpApiHost }}
  - Control Plane API:    https://{{ $cpApiHost }}
{{- else }}
  - Control Plane API:    kubectl get ingress control-plane-api-ingress -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].{{ $ipField }}}'
{{- end }}
{{- $cpAppHost := include "neuraltrust-platform.ingress.host" (dict "host" (dig "controlPlane" "components" "app" "host" "" $cpApp) "prefix" "app" "global" .Values.global) }}
{{- if $cpAppHost }}
  - Control Plane App:    https://{{ $cpAppHost }}
{{- else }}
  - Control Plane App:    kubectl get ingress control-plane-app-ingress -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].{{ $ipField }}}'
{{- end }}
{{- if .Values.trustlens.enabled }}
{{- $tlHost := include "neuraltrust-platform.ingress.host" (dict "host" (dig "ingress" "controlPlane" "host" "" (.Values.trustlens | default dict)) "prefix" "trustlens" "global" .Values.global) }}
{{- if $tlHost }}
  - TrustLens:            https://{{ $tlHost }}
{{- else }}
  - TrustLens:            kubectl get ingress trustlens -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].{{ $ipField }}}'
{{- end }}
{{- end }}
{{- end }}
{{- if $dataPlaneEnabled }}
{{- $dpApiHost := include "neuraltrust-platform.ingress.host" (dict "host" (dig "dataPlane" "components" "api" "host" "" $dpValues) "prefix" "data-plane-api" "global" .Values.global) }}
{{- if $dpApiHost }}
  - Data Plane API:       https://{{ $dpApiHost }}
{{- else }}
  - Data Plane API:       kubectl get ingress data-plane-api -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].{{ $ipField }}}'
{{- end }}
{{- end }}

{{- if $isSaas }}
{{- $db := .Values.databridge | default dict }}
{{- $tgVals := .Values.trustguard | default dict }}
{{- $dbMinting := and (eq (include "neuraltrust-platform.boolish" (dict "value" (dig "tls" "autoGenerate" true $db) "default" true)) "true") (not (dig "tls" "existingSecret" "" $db | toString | trim)) }}
{{- $agMinting := and (eq (include "neuraltrust-platform.boolish" (dict "value" (dig "configSync" "expose" "selfSignedTls" true $ag) "default" true)) "true") (not (dig "configSync" "grpcTls" "existingSecret" "" $ag | toString | trim)) }}
{{- $tgMinting := and (eq (include "neuraltrust-platform.boolish" (dict "value" (dig "configSync" "expose" "selfSignedTls" true $tgVals) "default" true)) "true") (not (dig "configSync" "grpcTls" "existingSecret" "" $tgVals | toString | trim)) }}
Remote data-plane endpoints (L4 names must bypass any TLS-terminating proxy):
  - DataBridge:               {{ include "neuraltrust-platform.saas.databridgeHost" . }}:443
  - Telemetry (HTTPS):        {{ include "neuraltrust-platform.saas.telemetryEndpoint" . }}
  - AgentGateway config-sync: {{ include "neuraltrust-platform.configSync.publicHost" (dict "ctx" . "product" "agentgateway") }}:443
  - TrustGuard config-sync:   {{ include "neuraltrust-platform.configSync.publicHost" (dict "ctx" . "product" "trustguard") }}:443
  kubectl get svc -n {{ .Release.Namespace }} databridge-southbound agentgateway-admin-configsync trustguard-control-plane-configsync
{{- if or $dbMinting $agMinting $tgMinting }}
  Warning: DataBridge and/or config-sync are serving chart-minted certificates. Remote planes reject the handshake until you install the generated CA (scripts/export-controlplane-ca.sh → global.controlPlane.caSecretName).
{{- end }}
{{- end }}

{{- if eq $platform "aws" }}
  Note: It may take a few minutes for ALB URLs to be provisioned by the AWS Load Balancer Controller.
{{- else if eq $platform "gcp" }}
  Note: It may take 3-5 minutes for the Google Cloud Load Balancer to provision an external IP.
{{- else if eq $platform "azure" }}
  Note: It may take a few minutes for the Azure Application Gateway to provision.
{{- else }}
  Note: It may take a few minutes for the load balancer to provision external endpoints.
{{- end }}

To get the status of your deployment, run:
  kubectl get pods -n {{ .Release.Namespace }}

For more information, see the README.md file.
