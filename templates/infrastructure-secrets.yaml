{{- if .Values.neuraltrust.dataPlane.enabled }}
---
# ClickHouse connection configuration for NeuralTrust Data Plane
# This template ensures clickhouse-secrets has the correct values based on infrastructure configuration
# The actual secret is created by the neuraltrust-data-plane subchart, but we need to ensure
# the values are passed correctly. The subchart will use dataPlane.components.clickhouse.* values
{{- end }}

