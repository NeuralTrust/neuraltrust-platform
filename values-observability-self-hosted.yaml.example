# Platform v2 external with local-only observability.
# The umbrella OTel Collector stays local. The external-only ClickStack
# collector writes product telemetry to ClickHouse.

global:
  platformVersion: "v2"
  deploymentMode: "external"
  observability:
    enabled: true
    hostedExport:
      enabled: false
    sendLogs: false
    sendTraces: true
    collector:
      endpoint: ""
      bufferPVC:
        enabled: false
  monitoring:
    enabled: true
    additionalLabels:
      release: "kube-prometheus-stack"
    interval: 30s

alertengine:
  enabled: true

neuraltrust-watchdog:
  enabled: true
  actions:
    dryRun: true
  enabledCheckIds:
    - clickhouse
    - firewall-health
    - pod-health
    - otel-collector
  monitoring:
    scrapeAnnotations:
      enabled: true
    podMonitor:
      enabled: true
      additionalLabels:
        release: "kube-prometheus-stack"
    prometheusRule:
      enabled: true
