# Platform v2 external: self-hosted control/data planes and analytics.
# ClickStack OTel Collector, DataCore, and AlertEngine render only in this mode.

global:
  deploymentMode: "external"
  platform: "kubernetes"
  domain: "platform.example.com"
  imageRegistry: ""
  observability:
    hostedExport:
      enabled: false

infrastructure:
  clickhouse:
    deploy: true

alertengine:
  enabled: true

trustlens:
  enabled: false
